export interface HeroSectionType {
    node: {
        title: string,
        slug: string,
        excerpt: string,
        buttonText: string
        backgroundImage: {
            url: string
        }
    }
}